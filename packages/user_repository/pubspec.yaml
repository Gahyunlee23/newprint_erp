name: user_repository
description: Dart package which manages the user domain.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  equatable: ^2.0.0
  dio: ^5.5.0+1
  flutter_riverpod: ^1.0.0
  shared_utils:
    path: ../shared_utils